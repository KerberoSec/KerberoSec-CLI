// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: kerberosec/checkpoints.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Empty, EmptyRequest, Int64, Metadata as Metadata1 } from "./common";

export interface CheckpointRestoreRequest {
  metadata: Metadata1 | undefined;
  number: number;
  restoreType: string;
  offset?: number | undefined;
}

function createBaseCheckpointRestoreRequest(): CheckpointRestoreRequest {
  return { metadata: undefined, number: 0, restoreType: "", offset: undefined };
}

export const CheckpointRestoreRequest: MessageFns<CheckpointRestoreRequest> = {
  encode(message: CheckpointRestoreRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.metadata !== undefined) {
      Metadata1.encode(message.metadata, writer.uint32(10).fork()).join();
    }
    if (message.number !== 0) {
      writer.uint32(16).int64(message.number);
    }
    if (message.restoreType !== "") {
      writer.uint32(26).string(message.restoreType);
    }
    if (message.offset !== undefined) {
      writer.uint32(32).int64(message.offset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckpointRestoreRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckpointRestoreRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.metadata = Metadata1.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.number = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.restoreType = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.offset = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CheckpointRestoreRequest {
    return {
      metadata: isSet(object.metadata) ? Metadata1.fromJSON(object.metadata) : undefined,
      number: isSet(object.number) ? globalThis.Number(object.number) : 0,
      restoreType: isSet(object.restoreType)
        ? globalThis.String(object.restoreType)
        : isSet(object.restore_type)
        ? globalThis.String(object.restore_type)
        : "",
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : undefined,
    };
  },

  toJSON(message: CheckpointRestoreRequest): unknown {
    const obj: any = {};
    if (message.metadata !== undefined) {
      obj.metadata = Metadata1.toJSON(message.metadata);
    }
    if (message.number !== 0) {
      obj.number = Math.round(message.number);
    }
    if (message.restoreType !== "") {
      obj.restoreType = message.restoreType;
    }
    if (message.offset !== undefined) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CheckpointRestoreRequest>, I>>(base?: I): CheckpointRestoreRequest {
    return CheckpointRestoreRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CheckpointRestoreRequest>, I>>(object: I): CheckpointRestoreRequest {
    const message = createBaseCheckpointRestoreRequest();
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata1.fromPartial(object.metadata)
      : undefined;
    message.number = object.number ?? 0;
    message.restoreType = object.restoreType ?? "";
    message.offset = object.offset ?? undefined;
    return message;
  },
};

export type CheckpointsServiceDefinition = typeof CheckpointsServiceDefinition;
export const CheckpointsServiceDefinition = {
  name: "CheckpointsService",
  fullName: "kerberosec.CheckpointsService",
  methods: {
    checkpointRestore: {
      name: "checkpointRestore",
      requestType: CheckpointRestoreRequest as typeof CheckpointRestoreRequest,
      requestStream: false,
      responseType: Empty as typeof Empty,
      responseStream: false,
      options: {},
    },
    /**
     * Opens a multi-file diff of everything that changed between the latest
     * checkpoint (snapshotted when the user's last message started a run) and
     * the current working tree.
     */
    checkpointViewLatestChanges: {
      name: "checkpointViewLatestChanges",
      requestType: EmptyRequest as typeof EmptyRequest,
      requestStream: false,
      responseType: Empty as typeof Empty,
      responseStream: false,
      options: {},
    },
    /**
     * Returns how many files changed between the latest checkpoint and the
     * current working tree (0 when no checkpoint exists). Used to enable the
     * "View Changes" button only when there is something to show.
     */
    checkpointLatestChangesCount: {
      name: "checkpointLatestChangesCount",
      requestType: EmptyRequest as typeof EmptyRequest,
      requestStream: false,
      responseType: Int64 as typeof Int64,
      responseStream: false,
      options: {},
    },
  },
} as const;

export type CheckpointsServiceService = typeof CheckpointsServiceService;
export const CheckpointsServiceService = {
  checkpointRestore: {
    path: "/kerberosec.CheckpointsService/checkpointRestore" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CheckpointRestoreRequest): Buffer =>
      Buffer.from(CheckpointRestoreRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CheckpointRestoreRequest => CheckpointRestoreRequest.decode(value),
    responseSerialize: (value: Empty): Buffer => Buffer.from(Empty.encode(value).finish()),
    responseDeserialize: (value: Buffer): Empty => Empty.decode(value),
  },
  /**
   * Opens a multi-file diff of everything that changed between the latest
   * checkpoint (snapshotted when the user's last message started a run) and
   * the current working tree.
   */
  checkpointViewLatestChanges: {
    path: "/kerberosec.CheckpointsService/checkpointViewLatestChanges" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: EmptyRequest): Buffer => Buffer.from(EmptyRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): EmptyRequest => EmptyRequest.decode(value),
    responseSerialize: (value: Empty): Buffer => Buffer.from(Empty.encode(value).finish()),
    responseDeserialize: (value: Buffer): Empty => Empty.decode(value),
  },
  /**
   * Returns how many files changed between the latest checkpoint and the
   * current working tree (0 when no checkpoint exists). Used to enable the
   * "View Changes" button only when there is something to show.
   */
  checkpointLatestChangesCount: {
    path: "/kerberosec.CheckpointsService/checkpointLatestChangesCount" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: EmptyRequest): Buffer => Buffer.from(EmptyRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): EmptyRequest => EmptyRequest.decode(value),
    responseSerialize: (value: Int64): Buffer => Buffer.from(Int64.encode(value).finish()),
    responseDeserialize: (value: Buffer): Int64 => Int64.decode(value),
  },
} as const;

export interface CheckpointsServiceServer extends UntypedServiceImplementation {
  checkpointRestore: handleUnaryCall<CheckpointRestoreRequest, Empty>;
  /**
   * Opens a multi-file diff of everything that changed between the latest
   * checkpoint (snapshotted when the user's last message started a run) and
   * the current working tree.
   */
  checkpointViewLatestChanges: handleUnaryCall<EmptyRequest, Empty>;
  /**
   * Returns how many files changed between the latest checkpoint and the
   * current working tree (0 when no checkpoint exists). Used to enable the
   * "View Changes" button only when there is something to show.
   */
  checkpointLatestChangesCount: handleUnaryCall<EmptyRequest, Int64>;
}

export interface CheckpointsServiceClient extends Client {
  checkpointRestore(
    request: CheckpointRestoreRequest,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
  checkpointRestore(
    request: CheckpointRestoreRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
  checkpointRestore(
    request: CheckpointRestoreRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
  /**
   * Opens a multi-file diff of everything that changed between the latest
   * checkpoint (snapshotted when the user's last message started a run) and
   * the current working tree.
   */
  checkpointViewLatestChanges(
    request: EmptyRequest,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
  checkpointViewLatestChanges(
    request: EmptyRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
  checkpointViewLatestChanges(
    request: EmptyRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
  /**
   * Returns how many files changed between the latest checkpoint and the
   * current working tree (0 when no checkpoint exists). Used to enable the
   * "View Changes" button only when there is something to show.
   */
  checkpointLatestChangesCount(
    request: EmptyRequest,
    callback: (error: ServiceError | null, response: Int64) => void,
  ): ClientUnaryCall;
  checkpointLatestChangesCount(
    request: EmptyRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Int64) => void,
  ): ClientUnaryCall;
  checkpointLatestChangesCount(
    request: EmptyRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Int64) => void,
  ): ClientUnaryCall;
}

export const CheckpointsServiceClient = makeGenericClientConstructor(
  CheckpointsServiceService,
  "kerberosec.CheckpointsService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): CheckpointsServiceClient;
  service: typeof CheckpointsServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
