---
title: "KerberoSec SDK"
sidebarTitle: "Overview"
description: "TypeScript packages for embedding KerberoSec's agent runtime in your own applications."
---

The KerberoSec SDK is an open source framework for building agentic applications, and is the same harness used in the KerberoSec IDE extensions and CLI. It uses a plugin architecture that makes it easy to customize and comes with all the features you expect from agents like checkpoints, web fetch, MCPs, cron jobs, subagents, and more.

Use the KerberoSec SDK to run agents from CI/CD pipelines, create automations for end-to-end workflows, or embed agents directly inside your products.

## Install

```bash
npm install @kerberosec/sdk
```

`@kerberosec/sdk` exports all SDK packages: `@kerberosec/core` for the full agent harness, `@kerberosec/agents` for the stateless agent loop, `@kerberosec/llms` for control over the model gateway, and `@kerberosec/shared` for common utilities.

Requires Node.js 22 or later.

## SDK Skill

If you use a coding agent (Claude Code, Codex, KerberoSec, etc.), install the [KerberoSec SDK skill](https://github.com/kerberosec/sdk-skill) to give your agent context on the SDK's APIs and best practices to help you build with the KerberoSec SDK.

```bash
npx skills add kerberosec/sdk-skill
```

Prompt it to scaffold agents, create custom tools, wire up plugins, configure providers, and more.

## Your First Agent

```typescript
import { Agent } from "@kerberosec/sdk"

const agent = new Agent({
  providerId: "anthropic",
  modelId: "claude-sonnet-4-6",
  apiKey: process.env.ANTHROPIC_API_KEY,
  maxIterations: 1,
})

agent.subscribe((event) => {
  if (event.type === "assistant-text-delta") {
    process.stdout.write(event.text ?? "")
  }
})

const result = await agent.run("Explain what an SDK is in two sentences.")
```

<Note>
  Here is a complete [quickstart example](https://github.com/kerberosec/kerberosec/tree/main/apps/examples/quickstart). Clone it and run `bun dev` to try it.
</Note>

## Packages

| Package | Purpose |
|---------|---------|
| `@kerberosec/sdk` | Public SDK surface (re-exports `@kerberosec/core`) |
| `@kerberosec/core` | Node runtime for sessions, built-in tools, persistence, hub support, automation |
| `@kerberosec/agents` | Browser-compatible stateless agent execution loop |
| `@kerberosec/llms` | Provider gateway and model catalogs |
| `@kerberosec/shared` | Types, schemas, tool helpers, hooks, storage helpers |



See [Packages](/sdk/architecture/overview) for package boundaries and exports.

## Next Steps

<CardGroup cols={2}>
  <Card title="Examples" icon="rocket" href="/sdk/examples">
    Browse complete, runnable SDK examples.
  </Card>
  <Card title="Plugins" icon="diagram-project" href="/sdk/plugins">
    Extend KerberoSec's functionality.
  </Card>
  <Card title="Tools" icon="wrench" href="/sdk/tools">
    Add actions the model can call.
  </Card>
  <Card title="Building an Agent" icon="code" href="/sdk/guides/building-an-agent">
    Build a complete SDK agent from a tutorial.
  </Card>
</CardGroup>
